---
phase: 29-season-medals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/medals.ts
  - src/app/(app)/progression/medals/page.tsx
  - src/components/progression/medals/MedalsOverview.tsx
  - src/components/progression/medals/MedalCategoryCard.tsx
autonomous: false
---

<objective>
## Goal
Build dedicated `/progression/medals` page — 12 lifetime achievement medals across 4 categories (Dedication / Skill / Diversity / Resilience), 3 tiers each (Bronze / Silver / Gold). All-time scoped (schema lacks season tag).

## Purpose
Bigger, one-shot achievements that don't fit the chain or mastery models — they reward overall play patterns over a long horizon.
</objective>

<acceptance_criteria>

## AC-1: computeMedals returns 4 categories × 3 tiers
```gherkin
Given matches with kills/deaths/assists/win/champion_name/gold_diff_at_10/game_duration_seconds
When computeMedals(matches) runs
Then output is { categories: MedalCategory[] (4), totals: { earned, total, byTier: { bronze, silver, gold } } }
And each category includes id, label, currentValue, valueLabel, medals: { tier, threshold, earned }[]
```

## AC-2: Page Renders Overview + 4 Category Cards
```gherkin
Given the user has matches
When /progression/medals loads
Then MedalsOverview shows total earned + by-tier counts; 4 MedalCategoryCard render each with progress + 3 tier pips
```

## AC-3: Empty State
```gherkin
Given a user with 0 matches
When the page loads
Then a centred empty state shows + all categories still render in unearned state
```

## AC-4: Mobile Functional at 390px
```gherkin
Given the page at 390px
When loaded
Then categories stack 1-col; tier pips fit horizontally
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Build src/lib/medals.ts</name>
  <files>src/lib/medals.ts</files>
  <action>
    New module:
    - `MedalTier` type: 'bronze' | 'silver' | 'gold'
    - `MEDAL_CATEGORIES` constant array of 4 category definitions:
      1. Dedication — currentValue = totalGames; thresholds 100/500/1000; valueLabel "games"
      2. Skill — currentValue = lifetime avg KDA; thresholds 2.0/3.0/4.0; valueLabel "avg KDA"
      3. Diversity — currentValue = unique champions; thresholds 10/30/50; valueLabel "champions"
      4. Resilience — currentValue = comeback wins (gold_diff_at_10 ≤ -500 AND win); thresholds 10/25/50; valueLabel "comeback wins"
    - `computeMedals(matches)` — computes all 4 categories, returns full payload
    - Avg KDA = (totalK + totalA) / max(totalD, 1) across all matches
  </action>
  <verify>tsc 0 errors</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Build MedalsOverview + MedalCategoryCard</name>
  <files>
    src/components/progression/medals/MedalsOverview.tsx,
    src/components/progression/medals/MedalCategoryCard.tsx
  </files>
  <action>
    MedalsOverview (RSC): 4 stat cards — Total Earned (X/12), Bronze, Silver, Gold counts.

    MedalCategoryCard (RSC): per-category card with:
    - Category label header
    - Current value display (e.g., "Avg KDA: 3.2")
    - Progress bar to next tier (or "All gold!" when complete)
    - 3 medal pips horizontally (Bronze/Silver/Gold) with tier name + threshold below
    - Earned pips coloured by tier (amber/silver-gray/gold), unearned greyed
  </action>
  <verify>tsc 0 errors</verify>
  <done>Components compile</done>
</task>

<task type="auto">
  <name>Task 3: Build /progression/medals page</name>
  <files>src/app/(app)/progression/medals/page.tsx</files>
  <action>
    RSC page. Fetch matches, run computeMedals, render overview + 4 category cards (1-col mobile, 2-col sm+).
    Empty state when 0 matches.
    Footer note: "All-time scope — Riot doesn't tag matches by season."
  </action>
  <verify>Visit /progression/medals; verify all 4 categories render</verify>
  <done>AC-2, AC-3, AC-4 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Season Medals page with 4 categories × 3 tiers</what-built>
  <how-to-verify>Visit /progression/medals; verify all categories + earned pips render correctly</how-to-verify>
  <resume-signal>"approved"</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- BADGE_DEFS / chain logic
- Mastery page

## SCOPE LIMITS
- All-time scope (no season tag in schema)
- 4 categories × 3 tiers fixed (12 medals)
- No SVG art (Phase 46)
- No nav links (Phase 47)
- No new packages
</boundaries>

<verification>
- [ ] tsc 0 errors
- [ ] eslint 0 errors
- [ ] Page renders at 390px
</verification>

<output>
After approval, create `.paul/phases/29-season-medals/29-01-SUMMARY.md`
</output>
