---
phase: 58-hub-query-infra
plan: 01
type: execute
wave: 2
depends_on: [53-01]
scope: logic-only  # UI deferred to Phase 65 (Build Hub UI)
files_modified:
  - supabase/migrations/20260426000000_hub_search_indexes.sql
  - src/lib/builds/hub-query.ts
  - src/lib/builds/search-index.ts
  - src/lib/builds/similarity.ts
  - src/lib/builds/saved-searches.ts
  - src/lib/builds/actions.ts
  - src/lib/types/builds.ts
  - src/lib/builds/hub-query.test.ts
  - src/lib/builds/similarity.test.ts
autonomous: false
---

<objective>
Hub query infrastructure: Postgres full-text search (tsvector + trigram), facet/sort query builders, build-similarity scoring (for "builds like this one"), saved-search CRUD, and autocomplete endpoint. No hub UI — Phase 65 consumes this contract.
</objective>

<scope>
**IN:**
- Migration enabling `pg_trgm` (safe if already enabled; `CREATE EXTENSION IF NOT EXISTS`); adding `search_tsv tsvector` column on `custom_builds` with generated expression over `name || description_md || tags || combos || warding_note`; GIN index on `search_tsv`; trigram GIN index on `name`; additional indexes the filter needs
- Table `saved_searches` (id, user_id, name, query_json, created_at) with RLS
- `hub-query.ts` — `listPublicBuilds({ q, filters, sort, page })` server helper. Filters supported:
  - champion[]
  - role[]
  - patch[] (multi-select)
  - tag[]
  - keystone rune id
  - spell pair
  - contains-item[]
  - has-matchup-note-vs[]
  - matchup-difficulty[]
  - rank-floor (Diamond+, Platinum+, etc. — reads creator's peak rank from summoner_profiles when Phase 61 lands; stubbed now)
  - champion-pool (accepts user id; joins to their owned champions; returns only matching)
  - freshness (current-patch, last-2, any)
  - has-aggregate-wr (requires opt_in_aggregate + ≥ N match tags)
- Sort options: `newest`, `most-bookmarked`, `aggregate-wr`, `activity-7d`, `fresh-on-patch`
- URL-serialization helper: `serializeHubQuery(query) → searchParams string` + `parseHubQuery(searchParams) → query`
- `search-index.ts` — `refreshSearchIndex(buildId)` trigger wrapper (called on save to keep tsvector fresh; Postgres generated column handles most cases, but this helps for complex triggers if needed)
- `similarity.ts` — `buildVector(build): ItemSet` + `cosineSimilarity(a, b)` over item multisets; `findSimilar(buildId, limit)`
- `saved-searches.ts` + actions `saveSearch(name, query)`, `listSavedSearches()`, `deleteSavedSearch(id)`
- Autocomplete helper `autocompleteHubSearch(prefix): { champions, items, tags }` — hits Data Dragon catalogues + top tag frequencies

**OUT (→ Phase 65 UI):**
- `/builds/hub` route and any page component
- `/builds/hub/[id]` public detail page
- `SearchBar`, `FilterSidebar`, `SortDropdown`, `SavedSearches`, `SimilarBuilds` components
- URL state wiring in the UI
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Migration — indexes + saved_searches</name>
  <files>supabase/migrations/20260426000000_hub_search_indexes.sql</files>
  <action>
    - `CREATE EXTENSION IF NOT EXISTS pg_trgm`
    - Alter `custom_builds` to add generated `search_tsv tsvector` column based on `setweight(to_tsvector('simple', coalesce(name,'')), 'A') || setweight(to_tsvector('simple', coalesce(description_md,'')), 'B') || setweight(to_tsvector('simple', array_to_string(tags, ' ')), 'A') || setweight(to_tsvector('simple', array_to_string(combos, ' ')), 'C')`
    - `CREATE INDEX ... ON custom_builds USING GIN (search_tsv)`
    - Trigram index on `name`: `CREATE INDEX ... ON custom_builds USING GIN (name gin_trgm_ops)`
    - Supporting btree indexes: `(champion_id, is_public)`, `(patch_tag, is_public)`, `(is_public, created_at desc)`, `(is_public, updated_at desc)`
    - `saved_searches` table + RLS
    - Materialized view `build_bookmark_counts` (build_id, count) refreshed on bookmark CRUD (or via on-demand aggregation — simpler for v1)
  </action>
</task>

<task type="auto">
  <name>Task 2: Hub query builder</name>
  <files>src/lib/builds/hub-query.ts, src/lib/builds/hub-query.test.ts</files>
  <action>
    Server-only. `listPublicBuilds(query)` composes Supabase PostgREST chain or raw SQL (`.rpc` call if needed). Returns `{ results: HubBuildCard[], total, facets }` where `facets` reports counts per champion / role / patch / tag for the current filter state.
    Tests (unit, against fixture shapes — not DB): correctness of URL serialize/parse round-trip, filter-to-query-string builder produces expected PostgREST params.
  </action>
</task>

<task type="auto">
  <name>Task 3: Similarity</name>
  <files>src/lib/builds/similarity.ts, src/lib/builds/similarity.test.ts</files>
  <action>
    `buildVector` maps each build's Early + Core + Boots blocks into a multiset of `{ itemId: count }`. `cosineSimilarity` computes cosine over common-keyspace vectors. `findSimilar(buildId, limit=6)` loads candidate builds of the same champion + role + recent patches, scores, returns top N.
    Tests: identical → 1.0, disjoint → 0, partial overlap scores between.
  </action>
</task>

<task type="auto">
  <name>Task 4: Saved searches + autocomplete</name>
  <files>src/lib/builds/saved-searches.ts, src/lib/builds/actions.ts, src/lib/types/builds.ts</files>
  <action>
    3 server actions + types `SavedSearch`, `HubQuery`, `HubSort`, `HubFacets`.
    Autocomplete pulls from cached Data Dragon catalogues (already in `items-catalogue.ts`) + DB query `SELECT tag, COUNT(*) FROM (SELECT unnest(tags)...)` top-20.
  </action>
</task>
</tasks>

<verification>
- `npm test` — +~10 tests pass
- `npx tsc --noEmit` — 0 errors
- Migration applies cleanly on fresh local DB
- Manual: insert 5 public builds → `listPublicBuilds({ q: 'bruiser' })` returns hits; `findSimilar(id)` returns siblings
- Facet counts match manual queries
</verification>

<output>58-01-SUMMARY.md with query DSL example + performance notes (indexes verified via EXPLAIN).</output>
