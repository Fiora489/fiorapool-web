---
phase: 41-map-awareness-score
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/map-awareness.ts
  - src/app/(app)/coaching/map-awareness/page.tsx
  - src/components/coaching/map-awareness/AwarenessScoreCard.tsx
  - src/components/coaching/map-awareness/AwarenessFactors.tsx
  - src/components/coaching/map-awareness/VisionTrend.tsx
  - src/components/coaching/map-awareness/AwarenessTips.tsx
autonomous: false
---

<objective>
## Goal
Build dedicated `/coaching/map-awareness` page — composite Map Awareness Score (0-100), 3-factor breakdown (vision/min vs role target, wards placed/min, wards killed/min), 10-game vision trend line, contextual tips.

## Purpose
Complement Phase 20's vision dashboard with an action-focused coaching view: one number, what's hurting it, and what to do about it.
</objective>

<acceptance_criteria>

## AC-1: computeMapAwareness returns full payload
```gherkin
Given matches with vision data
When computeMapAwareness(matches) runs
Then output is { score, tier, factors, trend, tips, totalMatches, lowConfidence }
```

## AC-2: Page renders 4 sections
```gherkin
When /coaching/map-awareness loads with data
Then AwarenessScoreCard, AwarenessFactors, VisionTrend, AwarenessTips all render
```

## AC-3: Empty + Mobile
```gherkin
Given 0 matches or mobile 390px
Then empty state or responsive layout
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Build src/lib/map-awareness.ts</name>
  <files>src/lib/map-awareness.ts</files>
  <action>
    Role-weighted targets (same as Phase 20 vision benchmarks):
    TOP 1.0, JUNGLE 1.4, MIDDLE 1.1, BOTTOM 1.2, UTILITY 2.6, SUPPORT 2.6.

    Factors:
    - visionPerMin (50%): current vs role-weighted target. score = ratio-based (0 → 0, 1.0 → 75, 1.5+ → 100)
    - wardsPlacedPerMin (30%): target ~0.5 wards/min non-support, ~1.5 for supports; role-weighted
    - wardsKilledPerMin (20%): target ~0.1 wards/min non-support, ~0.3 for supports; role-weighted

    Composite score weighted average. Tier: <40 Unaware / 40-59 Developing / 60-79 Aware / 80+ Hawk Eye

    Trend: last 10 matches' vision/min (timeline).

    Tips: generate 1-3 based on which factor is lowest. Hard-coded tip strings per factor.
  </action>
  <verify>tsc 0 errors</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Build 4 components</name>
  <files>
    src/components/coaching/map-awareness/AwarenessScoreCard.tsx,
    src/components/coaching/map-awareness/AwarenessFactors.tsx,
    src/components/coaching/map-awareness/VisionTrend.tsx,
    src/components/coaching/map-awareness/AwarenessTips.tsx
  </files>
  <action>
    AwarenessScoreCard (RSC): hero score + tier + low-confidence warning.
    AwarenessFactors (RSC): 3 factor cards (vision/min, wards placed/min, wards killed/min) with progress bars + delta.
    VisionTrend (RSC): 10-bar chart of last 10 games' vision/min.
    AwarenessTips (RSC): bullet list of 1-3 contextual tips.
  </action>
  <verify>tsc 0 errors</verify>
  <done>Components compile</done>
</task>

<task type="auto">
  <name>Task 3: Build page</name>
  <files>src/app/(app)/coaching/map-awareness/page.tsx</files>
  <action>
    RSC page. Fetch matches, run computeMapAwareness. Render 4 sections + formula footer.
  </action>
  <verify>Visit /coaching/map-awareness</verify>
  <done>AC-2, AC-3 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Map Awareness page with score, factors, trend, tips</what-built>
  <how-to-verify>Visit /coaching/map-awareness</how-to-verify>
  <resume-signal>"approved"</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Other pages / lib (including Phase 20 objectives)

## SCOPE LIMITS
- Role-weighted targets (same as Phase 20)
- No control ward tracking (no separate column)
- No roam response (no timeline)
- No nav links (Phase 47)
</boundaries>

<verification>
- [ ] tsc 0 errors
- [ ] eslint 0 errors
</verification>

<output>
After approval, create `.paul/phases/41-map-awareness-score/41-01-SUMMARY.md`
</output>
