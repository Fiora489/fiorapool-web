---
phase: 60-hub-social-layer
plan: 01
type: execute
wave: 2
depends_on: [53-01, 58-01]
scope: logic-only  # UI deferred to Phase 67 (Build Hub UI)
files_modified:
  - src/lib/builds/bookmarks.ts
  - src/lib/builds/collections.ts
  - src/lib/builds/creator-profile.ts
  - src/lib/builds/actions.ts
  - src/lib/types/builds.ts
  - src/lib/builds/bookmarks.test.ts
  - src/lib/builds/collections.test.ts
autonomous: false
---

<objective>
Logic + server actions for the social layer of the Build Hub: bookmark/star other users' builds, personal collections/playlists, and creator profile data. No UI — the Hub UI phase (67) will render against this contract.
</objective>

<scope>
**IN:**
- `bookmarks.ts` server helpers:
  - `listBookmarkedForUser(userId, { page, sort })`
  - `isBookmarked(userId, buildId)`
  - `getBookmarkCount(buildId)` (reads from materialized view created in Phase 58)
- `collections.ts` server helpers:
  - `listCollectionsForUser(userId)`
  - `getCollection(id, viewerUserId)` (owner-only; public collections deferred)
  - `listBuildsInCollection(collectionId)`
- `creator-profile.ts`:
  - `getCreatorProfile(username)` — reads summoner profile + aggregates build count, total bookmarks, top champions
  - `listCreatorBuilds(username, { page, filters })` — public builds only
- Server actions in `actions.ts`:
  - `bookmarkBuild(buildId)` / `unbookmarkBuild(buildId)`
  - `createCollection(name, description?)`
  - `renameCollection(id, name)`
  - `setCollectionDescription(id, description)`
  - `deleteCollection(id)`
  - `addToCollection(collectionId, buildId)`
  - `removeFromCollection(collectionId, buildId)`
  - `reorderCollection(collectionId, buildIdsOrdered[])`
- Types `BookmarkRow`, `CollectionSummary`, `CollectionWithBuilds`, `CreatorProfile`
- Creator username lookup: add `username` column to `summoner_profiles` if absent (migration); default to `riot_id#tagline` slug. RLS public read for profiles.

**OUT (→ Phase 67 UI):**
- `/builds/bookmarked` page
- `/builds/collections` page + `/builds/collections/[id]` detail
- `/creators/[username]` page
- Bookmark button UI on build cards
- Collections reorder drag UI
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Migration (username on profiles + bookmark counts view)</name>
  <files>supabase/migrations/20260428000000_hub_social.sql</files>
  <action>
    - Add `username text unique` to `summoner_profiles` (populate default from slug)
    - Confirm `build_bookmark_counts` MV exists from Phase 58; if not, create here
    - Index on `build_bookmarks (build_id)` and `(user_id, created_at desc)`
    - RLS: `summoner_profiles` public read for `username, region, summoner_level` only; full row readable by owner
  </action>
</task>

<task type="auto">
  <name>Task 2: Bookmark + collection helpers</name>
  <files>src/lib/builds/bookmarks.ts, src/lib/builds/collections.ts, src/lib/builds/bookmarks.test.ts, src/lib/builds/collections.test.ts</files>
  <action>
    Implement helpers listed in scope.
    Unit tests on query builders (filter/sort translation only, not DB hits).
  </action>
</task>

<task type="auto">
  <name>Task 3: Creator profile helper</name>
  <files>src/lib/builds/creator-profile.ts</files>
  <action>
    Aggregation query: build count, total bookmarks across public builds, top 5 champions by build count, earliest public build date.
  </action>
</task>

<task type="auto">
  <name>Task 4: Server actions + types</name>
  <files>src/lib/builds/actions.ts, src/lib/types/builds.ts</files>
  <action>
    10 actions. Each revalidates both `/builds/bookmarked` and `/creators/[username]` where appropriate.
  </action>
</task>
</tasks>

<verification>
- `npm test` — +~6 tests pass
- `npx tsc --noEmit` — 0 errors
- Migration applies cleanly
- Manual: bookmark a public build → `listBookmarkedForUser` returns it; `getBookmarkCount` increments; removing → decrements
- RLS: user A cannot bookmark on behalf of user B; user B cannot read user A's private collection
</verification>

<output>60-01-SUMMARY.md listing action surface + sample creator-profile shape.</output>
