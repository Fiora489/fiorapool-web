---
phase: 44-win-condition-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sankey.ts
  - src/app/(app)/visualisations/sankey/page.tsx
  - src/components/visualisations/sankey/SankeyDiagram.tsx
autonomous: false
---

<objective>
Build `/visualisations/sankey` — a 3-stage Sankey showing how games flow from early state → mid state → outcome. Stages: Lane @10 (ahead/even/behind) → Game Length (short/mid/long) → Result (win/loss).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Lib + SVG sankey</name>
  <files>src/lib/sankey.ts, src/components/visualisations/sankey/SankeyDiagram.tsx</files>
  <action>
    Lib: classify each match into (lane, length, result) triple; count flows.
    Sankey: 3 column-layers with rectangles (nodes) + cubic bezier paths (flows). Pure SVG, no library.
  </action>
</task>
<task type="auto">
  <name>Task 2: Page</name>
  <files>src/app/(app)/visualisations/sankey/page.tsx</files>
  <action>RSC page fetching matches + rendering</action>
</task>
</tasks>

<output>After ship, write 44-01-SUMMARY.md</output>
