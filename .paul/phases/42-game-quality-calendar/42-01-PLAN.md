---
phase: 42-game-quality-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/game-quality.ts
  - src/app/(app)/visualisations/calendar/page.tsx
  - src/components/visualisations/calendar/QualityHeatmap.tsx
  - src/components/visualisations/calendar/DayOfWeekBreakdown.tsx
  - src/components/visualisations/calendar/BestWorstDays.tsx
autonomous: false
---

<objective>
## Goal
Build `/visualisations/calendar` page with a 12-week heatmap of daily game quality scores, day-of-week averages, and top best/worst days.

## Purpose
Identify temporal play patterns. "When am I actually good?" — a visible answer drives smarter session scheduling.
</objective>

<acceptance_criteria>

## AC-1: computeGameQuality returns full payload
```gherkin
Given matches with win/kills/deaths/assists/cs
When computeGameQuality(matches) runs
Then output is { days: DayBucket[], dowAverages: DowAvg[7], bestDays, worstDays, totalGames, avgQuality }
```

## AC-2: Page renders 3 sections
```gherkin
When /visualisations/calendar loads
Then QualityHeatmap, DayOfWeekBreakdown, BestWorstDays all render
```

## AC-3: Empty + Mobile
```gherkin
Given 0 matches or mobile 390px
Then empty state or responsive layout
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Build src/lib/game-quality.ts</name>
  <files>src/lib/game-quality.ts</files>
  <action>
    Per-match quality score (0-100):
    - +50 if win (0 if loss)
    - +KDA bonus: min(kda / 5, 1) * 25   (KDA 5+ = +25)
    - +performance bonus: 25 × (a mix of CS/min and vision/min normalized)
      Simple proxy: CS/min vs 7.0 baseline → min(csPerMin/7, 1.5) * 12.5
                    Vision/min vs 1.5 baseline → min(visPerMin/1.5, 1.5) * 12.5

    Group matches by captured_at YYYY-MM-DD. Each day = avg quality.

    Build last-84 (12 × 7) days array. Fill empty days.

    dowAverages: Mon-Sun average across all matches in each weekday.

    bestDays: top 3 days by avg quality (min 2 games).
    worstDays: bottom 3 days by avg quality (min 2 games).
  </action>
  <verify>tsc 0 errors</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Build 3 components</name>
  <files>
    src/components/visualisations/calendar/QualityHeatmap.tsx,
    src/components/visualisations/calendar/DayOfWeekBreakdown.tsx,
    src/components/visualisations/calendar/BestWorstDays.tsx
  </files>
  <action>
    QualityHeatmap (RSC): GitHub-style contribution grid. 12 columns (weeks), 7 rows (Mon-Sun). Cell colour:
    - no games: zinc-800
    - 0-30: rose-600
    - 30-50: amber-600
    - 50-70: emerald-600
    - 70+: amber-400 (gold)
    Tooltip via title attribute showing date + quality + game count.

    DayOfWeekBreakdown (RSC): 7 bars with avg quality per weekday.

    BestWorstDays (RSC): 2 cards side by side — Best 3 and Worst 3 days with date + quality + games.
  </action>
  <verify>tsc 0 errors</verify>
  <done>Components compile</done>
</task>

<task type="auto">
  <name>Task 3: Build page</name>
  <files>src/app/(app)/visualisations/calendar/page.tsx</files>
  <action>
    RSC page. Fetch matches, run computeGameQuality. Render 3 sections.
  </action>
  <verify>Visit /visualisations/calendar</verify>
  <done>AC-2, AC-3 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Game Quality Calendar page with 12-week heatmap + weekday breakdown + best/worst days</what-built>
  <how-to-verify>Visit /visualisations/calendar</how-to-verify>
  <resume-signal>"approved"</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Other pages / lib

## SCOPE LIMITS
- 12-week fixed horizon
- Local-timezone day boundaries (user's browser)
- Quality formula simple; more sophisticated model is a future iteration
- No nav links (Phase 47)
</boundaries>

<verification>
- [ ] tsc 0 errors
- [ ] eslint 0 errors
</verification>

<output>
After approval, create `.paul/phases/42-game-quality-calendar/42-01-SUMMARY.md`
</output>
