---
phase: 37-role-passport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/role-passport.ts
  - src/app/(app)/coaching/role-passport/page.tsx
  - src/components/coaching/role-passport/MainRoleCard.tsx
  - src/components/coaching/role-passport/RoleComparisonTable.tsx
  - src/components/coaching/role-passport/RoleDetailCard.tsx
autonomous: false
---

<objective>
## Goal
Build `/coaching/role-passport` — comprehensive per-role performance profile across TOP/JGL/MID/BOT/SUP. Main role callout, strongest/weakest, side-by-side comparison table, and per-role detail cards with top champions.

## Purpose
"What's my true main role?" — the answer isn't always the one played most. This page surfaces WR + KDA + top champions across all roles played.
</objective>

<acceptance_criteria>

## AC-1: computeRolePassport returns full payload
```gherkin
Given matches
When computeRolePassport(matches) runs
Then output is { roles: RoleProfile[], mainRole, strongestRole, weakestRole, totalRoles }
And each RoleProfile has { role, games, wins, winRate, avgKda, avgCsPerMin, avgVisionPerMin, topChampions: {name, games, winRate}[] }
```

## AC-2: Page renders 3 sections
```gherkin
Given user matches
When /coaching/role-passport loads
Then MainRoleCard, RoleComparisonTable, and per-role detail cards all render
```

## AC-3: Empty + Mobile
```gherkin
Given 0 matches or mobile 390px
Then empty state or responsive layout
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Build src/lib/role-passport.ts</name>
  <files>src/lib/role-passport.ts</files>
  <action>
    Types: RoleId ('TOP' | 'JUNGLE' | 'MIDDLE' | 'BOTTOM' | 'UTILITY'), TopChampion, RoleProfile, RolePassportStats.

    Group matches by normalised role (SUPPORT → UTILITY). Skip null/unknown roles.

    Per role compute: games, wins, winRate, avgKda, avgCsPerMin, avgVisionPerMin, topChampions (top 3 by games with WR).

    Main role = most games.
    Strongest / weakest = highest / lowest WR where games ≥ 5 (skip Unknown). If <2 qualifying, both null.

    Return sorted roles by games desc.
  </action>
  <verify>tsc 0 errors</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Build 3 components</name>
  <files>
    src/components/coaching/role-passport/MainRoleCard.tsx,
    src/components/coaching/role-passport/RoleComparisonTable.tsx,
    src/components/coaching/role-passport/RoleDetailCard.tsx
  </files>
  <action>
    MainRoleCard (RSC): hero card with Main Role label (large) + games + WR + KDA + "played Y% of games" chip. Additional small cards for Strongest Role and Weakest Role if available.

    RoleComparisonTable (RSC): side-by-side table columns — Role / Games / WR / KDA / CS/min / Vision/min. Win-rate colour coded. Primary role row highlighted.

    RoleDetailCard (RSC): per-role card with the 4 stats + top 3 champions in that role with games + WR each.
  </action>
  <verify>tsc 0 errors</verify>
  <done>Components compile</done>
</task>

<task type="auto">
  <name>Task 3: Build page</name>
  <files>src/app/(app)/coaching/role-passport/page.tsx</files>
  <action>
    RSC page. Fetch matches, run computeRolePassport, render 3 sections.
  </action>
  <verify>Visit /coaching/role-passport</verify>
  <done>AC-2, AC-3 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Role Passport with main role card, comparison table, per-role detail cards</what-built>
  <how-to-verify>Visit /coaching/role-passport</how-to-verify>
  <resume-signal>"approved"</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Other pages / lib

## SCOPE LIMITS
- 5 roles only (SR normal positions)
- No champion icons (Phase 46)
- No nav links (Phase 47)
</boundaries>

<verification>
- [ ] tsc 0 errors
- [ ] eslint 0 errors
- [ ] Page renders
</verification>

<output>
After approval, create `.paul/phases/37-role-passport/37-01-SUMMARY.md`
</output>
