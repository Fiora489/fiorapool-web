---
phase: 56-editor-matchup-meta
plan: 01
type: execute
wave: 1
depends_on: [53-01]
scope: logic-only  # UI deferred to Phase 64
files_modified:
  - src/lib/builds/matchup.ts
  - src/lib/builds/conditionals.ts
  - src/lib/builds/tags.ts
  - src/lib/builds/warding.ts
  - src/lib/builds/patch-stamp.ts
  - src/lib/builds/description.ts
  - src/lib/builds/actions.ts
  - src/lib/types/builds.ts
  - src/lib/builds/matchup.test.ts
  - src/lib/builds/conditionals.test.ts
  - src/lib/builds/tags.test.ts
autonomous: false
---

<objective>
Domain logic for all build-level metadata: matchup notes (per-enemy difficulty + note + threats), conditional item swaps, free-form tags, warding/trinket recommendations, patch stamp + last-validated, and markdown description sanitization.
</objective>

<scope>
**IN:**
- `matchup.ts` — `validateMatchupNote(input)` (enemy champ id valid, difficulty in enum, note length ≤ 1000, threats[] length ≤ 10); `MatchupDifficulty` enum
- `conditionals.ts` — `validateConditionalSwap(input)` (condition_text length ≤ 160, from_item ≠ to_item, both valid item ids)
- `tags.ts` — `normalizeTag(raw)` (lowercase, trim, kebab-case, strip special chars); `validateTagList(tags[])` (max 8 tags per build, length ≤ 24 each)
- `warding.ts` — enum of supported trinket/ward categories + validator
- `patch-stamp.ts` — `currentPatch()` (reads from `src/lib/ddragon.ts` version resolver); `isStale(stampedPatch, currentPatch, threshold = 2)` pure
- `description.ts` — markdown sanitizer allowlisting the subset we render (headings, bold, italic, links with rel=nofollow, code). Use existing markdown pipeline if present; otherwise add `rehype-sanitize` via the shadcn install path
- Types: `BuildMatchupNote`, `BuildConditionalSwap`, `BuildTag`, `WardingCategory` unions
- Server actions in `actions.ts`:
  - `upsertMatchupNote(buildId, note)`
  - `deleteMatchupNote(buildId, enemyChampionId)`
  - `addConditionalSwap(buildId, swap)`
  - `removeConditionalSwap(buildId, swapId)`
  - `reorderConditionalSwaps(buildId, orderedIds[])`
  - `setBuildTags(buildId, tags[])`
  - `setBuildWarding(buildId, note, trinket)`
  - `setBuildDescription(buildId, markdown)`
  - `stampLastValidated(buildId, patch)`  // defaults to current patch
  - `setBuildPatchTag(buildId, patch)`  // rarely used; auto on create

**OUT (→ Phase 64 UI):**
- Matchup multi-select picker, per-matchup card UI
- Threat list UI editor
- Conditional swap rule builder UI
- Tag input with suggestions UI
- Warding dropdown UI
- Markdown description editor component
- Patch-tag stamp button UI
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Validators + pure helpers</name>
  <files>src/lib/builds/matchup.ts, src/lib/builds/conditionals.ts, src/lib/builds/tags.ts, src/lib/builds/warding.ts, src/lib/builds/patch-stamp.ts, src/lib/builds/description.ts, src/lib/builds/matchup.test.ts, src/lib/builds/conditionals.test.ts, src/lib/builds/tags.test.ts</files>
  <action>
    Implement all validators listed in scope, each with a handful of targeted tests (happy path + each failure mode).
    Description sanitizer: strip `<script>`, `<iframe>`, `onclick` handlers, `javascript:` URLs; allow standard markdown inline + block.
  </action>
</task>

<task type="auto">
  <name>Task 2: Types</name>
  <files>src/lib/types/builds.ts</files>
  <action>Add the types listed in scope. Keep mirrored to DB columns from Phase 53.</action>
</task>

<task type="auto">
  <name>Task 3: Server actions</name>
  <files>src/lib/builds/actions.ts</files>
  <action>
    Implement the 10 actions listed. Each: auth check, validate with the pure helpers, Supabase mutation, revalidate `/builds/custom/[id]/edit` and (if public) `/builds/hub/[id]`.
  </action>
</task>
</tasks>

<verification>
- `npm test` — +~12 tests pass
- `npx tsc --noEmit` — 0 errors
- Manual (via script or Supabase Studio): insert matchup note against Fiora vs Darius → row exists with difficulty='hard'; RLS: cannot read another user's note
- Markdown: description with `<script>alert(1)</script>` is stripped on save
</verification>

<output>56-01-SUMMARY.md listing action surface + validator rules matrix.</output>
