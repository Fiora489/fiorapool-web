---
phase: 57-editor-polish-logic
plan: 01
type: execute
wave: 1
depends_on: [54-01, 55-01, 56-01]
scope: logic-only  # UI deferred to Phase 64
files_modified:
  - src/lib/builds/lint.ts
  - src/lib/builds/dupe.ts
  - src/lib/builds/undo-redo.ts
  - src/lib/builds/block-templates.ts
  - src/lib/builds/actions.ts
  - src/lib/builds/lint.test.ts
  - src/lib/builds/dupe.test.ts
  - src/lib/builds/undo-redo.test.ts
autonomous: false
---

<objective>
Domain logic for editor-quality features: build lint rules, dupe detection, framework-agnostic undo/redo stack, and item-block template CRUD. All as pure/testable modules the Build Creator UI (Phase 64) will consume.
</objective>

<scope>
**IN:**
- `lint.ts` — rule engine `lintBuild(build) → LintWarning[]`. Rule interface `{ id, severity, check(build) }`. Built-in rules:
  - `no-mr-vs-ap`: no MR item when tagged `anti-ap` or ≥ 2 matchup notes flagged as AP casters
  - `missing-keystone`: rune page assigned but no keystone
  - `too-many-ad-for-ap-scaler`: ≥ 3 AD items for a champion tagged AP scaler
  - `boots-count`: exactly 1 boots item expected in Boots block
  - `final-build-size`: final build rows should total ≤ 6 items + boots
  - `situational-empty`: warn (severity: info) if no situational options for a public build
- `dupe.ts` — `scoreSimilarity(buildA, buildB): number 0..1` using Jaccard on canonical item sets from Phase 53's `canonicalBuildKey`, weighted by block priority (core > early > starting > boots > situational > full). `detectDupes(candidate, ownedBuilds[]): DupeMatch[]` thresholded at 0.9.
- `undo-redo.ts` — framework-agnostic stack `createHistory<T>({ limit: 20 })`. Methods: `push(state)`, `undo()`, `redo()`, `peek()`, `canUndo`, `canRedo`. Immutable snapshots; ignores pushes identical to top.
- `block-templates.ts` — CRUD helpers for `build_item_block_templates` table; `listTemplates(userId, championId?)`, `applyTemplate(buildId, blockType, templateId)` (server action wrapper).
- Server actions in `actions.ts`:
  - `saveBlockTemplate(championId, blockType, items[])`
  - `deleteBlockTemplate(id)`
  - `applyBlockTemplate(buildId, blockType, templateId)`

**OUT (→ Phase 64 UI):**
- `BuildLintPanel` component
- `DupeDetectionDialog` component
- Undo/redo button bar
- Item block template manager UI
- Mobile UX (long-press drag, swipe reorder) — that's implementation detail of the UI phase
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Lint engine</name>
  <files>src/lib/builds/lint.ts, src/lib/builds/lint.test.ts</files>
  <action>
    Implement the rule interface + 6 built-in rules. Each rule exports `{ id, severity: 'info' | 'warn' | 'error', message(ctx): string, check(build): boolean }`.
    Tests: one case per rule (positive + negative).
  </action>
</task>

<task type="auto">
  <name>Task 2: Dupe detection</name>
  <files>src/lib/builds/dupe.ts, src/lib/builds/dupe.test.ts</files>
  <action>
    `scoreSimilarity` + `detectDupes`. Tests:
    - identical builds → 1.0
    - disjoint builds → 0
    - same core, different situational → ≥ 0.85
    - same starting + boots but different core → < 0.6
  </action>
</task>

<task type="auto">
  <name>Task 3: Undo/redo stack</name>
  <files>src/lib/builds/undo-redo.ts, src/lib/builds/undo-redo.test.ts</files>
  <action>
    Pure. Tests:
    - push 3, undo 2 → peek is state 1
    - push 3, undo 2, push new → redo stack cleared
    - push > limit → oldest dropped
    - push equal to top → no-op
  </action>
</task>

<task type="auto">
  <name>Task 4: Block templates + actions</name>
  <files>src/lib/builds/block-templates.ts, src/lib/builds/actions.ts</files>
  <action>
    Helpers + 3 server actions. `applyBlockTemplate` copies the template's items into the build block and triggers autosave's equivalent path.
  </action>
</task>
</tasks>

<verification>
- `npm test` — +~14 tests pass
- `npx tsc --noEmit` — 0 errors
- Manual smoke: a build with 0 MR items + matchup note vs Syndra (AP) → `lintBuild` returns `no-mr-vs-ap` warning
- Lint surface ready for Phase 64 UI
</verification>

<output>57-01-SUMMARY.md with rule list + example lint output.</output>
