---
phase: 47-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens.css
  - src/app/globals.css
  - src/components/ui/PageHeader.tsx
  - src/components/ui/StatCard.tsx
  - src/components/ui/SectionHeader.tsx
  - src/components/ui/ScoreHero.tsx
  - src/components/nav.tsx
autonomous: false
---

<objective>
Final design pass — extract recurring UI patterns (PageHeader, SectionHeader, StatCard, ScoreHero) into reusable primitives under `src/components/ui/`. Add CSS design tokens for tier colours + spacing. Expand nav with grouped routes (Analytics / Coaching / Progression / Visualisations / Export).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Design tokens + primitives</name>
  <files>src/styles/tokens.css, src/app/globals.css, src/components/ui/PageHeader.tsx, src/components/ui/SectionHeader.tsx, src/components/ui/StatCard.tsx, src/components/ui/ScoreHero.tsx</files>
  <action>
    Tokens: extract tier colour classes (rose/amber/emerald/gold for tier tones) as CSS custom props.
    PageHeader: breadcrumb back link + title + description slot.
    SectionHeader: uppercase tracking label used across 30+ sections.
    StatCard: the label/value/sublabel pattern repeated ~40 times.
    ScoreHero: the tier + score + sublabel pattern from all composite-score pages.

    Components are lean primitives — no rewiring existing pages required; they're available for the next iteration.
  </action>
</task>
<task type="auto">
  <name>Task 2: Nav grouping</name>
  <files>src/components/nav.tsx</files>
  <action>Restructure nav into grouped sections with all 30 new pages accessible. Mobile-friendly (collapsible on small screens, full on md+).</action>
</task>
</tasks>

<output>After ship, write 47-01-SUMMARY.md — this closes v1.1</output>
